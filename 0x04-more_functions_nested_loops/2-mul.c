#include "main.h"
/**
  * mul - multiplies two integers by one another
  * @a: that's the first one
  * @b: that's the other
  * Return: the product of a and b
  */
int mul(int a, int b)
{
int product;

product = (a * b);
return (product);
}
