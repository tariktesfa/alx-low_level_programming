#include "main.h"

/**
 * _strlen - returns lenght of string
 * @s: input parameter
 *
 * Description: uses pointers
 * Return: Alawy (0)
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		continue;
	}

	return (i);
}

